html
   head
      title Form Tester
   body
      form(action = "/upload", method = "POST" enctype='multipart/form-data')
   

         label(for = "pro_pic") profile picture: 
         input(name = "image" type = "file" )
      
         input(type='submit')